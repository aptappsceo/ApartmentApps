@model dynamic
@(Html.EJ().Schedule("Schedule1")
        .Width("100%")
        .Height("525px")
        .CurrentDate(DateTime.Now)
        .AppointmentSettings(fields => fields.Datasource(ds=>ds.URL(Url.Action("MyScheduleData")))
            .Id("Id")
            .Subject("Title")
            .StartTime("ScheduleDate")
            .EndTime("EndDate")
            .Description("Comments")
            //.Recurrence("Recurrence")
            //.RecurrenceRule("RecurrenceRule")
            )
)