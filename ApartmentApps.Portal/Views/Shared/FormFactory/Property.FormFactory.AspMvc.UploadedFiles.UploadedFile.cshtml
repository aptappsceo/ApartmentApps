@using FormFactory
@using FormFactory.AspMvc
@model PropertyVm
@if(!Model.Readonly)
{
   
    <input type="file" class="form-control" id="@Model.Name" name="@Model.Name" @Html.Raw(Model.Readonly()) @Html.Raw(Model.Disabled()) />
}